import React from 'react';

const LoadingSpinner = () => {
  return (
    <div className='h-screen flex items-center justify-center'>
      <button class="btn loading btn-primary text-white">loading...</button>
    </div>
  );
};

export default LoadingSpinner;
